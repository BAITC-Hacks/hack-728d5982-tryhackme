# Used by app.landing. Copy to .env and set private values locally.
OPENAI_API_KEY=
AI_MODEL=gpt-4o-mini
EKT_API_USER=
EKT_API_PASSWORD=
# Optional: only for reasoning models; use a value supported by your model.
# AI_ASSISTANT_EFFORT=low

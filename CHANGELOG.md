# CHANGELOG

Источник требований: [итоговое Word ТЗ HackAlem AI — «ИИ-ассистент для чата на сайте ekt.kz»](<docs/source of truth/assets/HackAlem AI_ ИИ-ассистент для чата на сайте ekt.kz.docx>). Номера пунктов ниже относятся к разделу **7. Must have** этого документа. [PRD](<docs/source of truth/PRD.md>) раскрывает их как UC-01…05 и критерии приёмки.

## 2026-09-23

### Читаемость тёмного чата — UC-01…05

- Устранён светлый фон карточек товара со светлым текстом после переноса. Чат использует общие семантические цвета Tailwind v4/shadcn: фон, карточки, основной и вторичный текст, кнопки, поля и границы.
- Основной текст увеличен до 14 px, подписи — до 12–13 px; поля на телефоне — 16 px. Характеристики, предупреждения, корзина, подтверждение и черновик менеджера оформлены в одной теме.
- Production build, TypeScript и ESLint прошли. Восемь целевых Playwright-проверок на desktop/mobile подтвердили карточку, согласие, корзину после обновления, менеджера и отсутствие переполнения. Контраст текста и фона проверяется в браузере по порогу 4,5:1. Это автономные проверки, внешние AI/API не вызывались.

### Next.js-лендинг и общий чат — UC-01…05

- Лендинг перенесён на React / Next.js 16 с существующими Tailwind 4 и shadcn Button. Добавлены тёмная тема, тонкие движущиеся RGB-линии, адаптивные разделы, заметные действия и отключение анимации при `prefers-reduced-motion`.
- Для простого переноса рабочий HTML/JS чата извлекается из `index.html` при dev/build и подключается в корневом layout. Стили ограничены контейнером; чат оформлен в общей теме. Это повторное использование проверенного поведения, а не полный перенос сообщений на React. Кнопки включаются после загрузки обработчиков; окно остаётся одним.
- Next.js проксирует `/api` в FastAPI с cookie и CSRF, проверяет Origin, принимает вложения и обслуживает `/cart`. При проверке исправлено сравнение Origin с внутренним hostname Next.js: используется публичный Host, подставленный `X-Forwarded-Host` не принимается. Ключи остаются на backend.
- **Проверено:** production build, ESLint, TypeScript; **24 автономных браузерных проверки** на desktop/mobile. С реальными OpenAI/ekt.kz **9 из 10 сценариев прошли в отдельных запусках**: Excel, JPEG и помощь на обоих экранах; вся цепочка UC-01…05 на mobile; две проверки защиты прокси. Подтверждены отдельное согласие, актуальная корзина после обновления, изоляция сессий и отклонение чужого Origin/CSRF.
- **Незакрытая проверка:** полный desktop live UC не прошёл в двух попытках: backend вернул сообщение о сбое обработки без товаров (в первом прогоне — ошибка разбора ответа OpenAI). Повтор остановился на второй отсутствующей позиции; успешным его не считаем. Контролируемый браузерный сценарий и мобильный живой путь проходят. Backend и исходный `index.html` не изменены; устойчивость ответов модели остаётся отдельной задачей.
- Добавлены команды `make test-next-ui` / `make test-next-live`, автономный Next-набор в CI и инструкции запуска двух процессов. Новых зависимостей нет. Существующий Docker обслуживает прежний самостоятельный HTML; развёртывание Next.js отдельным сервисом ещё не выполнено. Входы и прошлые живые отчёты `out/` сохранены, обновлена инструкция запуска.

### Закупочные документы, полезные ответы и менеджер — UC-01…05, §8 ТЗ

- Вместо прежних `specification.*` в `out/inputs/` — восемь закупочных входов: ERP-заявка, письмо снабжения, проектная спецификация, PDF-скан/JPEG накладной, выгрузка CSV, TXT электрика и исходное фото аппарата. Реалистичные формы и количества смоделированы; реальные клиентские документы не использованы. Идеи `TMP_real_out_files.md` ограничены сценариями Word ТЗ.
- Excel/CSV передают модели структуру листов/строк и заголовки. Исправлены выявленные в первом живом прогоне поиск по названию вместо кода производителя и потеря повтора на другом листе. В разборе остаются неизвестные количества; цены чужого предложения не становятся ценами магазина.
- Системный промпт маршрутизирует вопросы о заказе, возможностях и менеджере, включая опечатки. Вместо пустых обещаний — конкретные шаги и следующий вопрос. `index.html` показывает обработку файлов, ожидание, подсказки и ссылку на исходный сайт при нехватке данных.
- Контакты менеджера читаются с официальной страницы; в одном чате доступны редактируемый черновик, копирование, телефон и WhatsApp. При сбое модели/каталога, конфликте свойств, неизвестном сертификате или неудачном подборе есть путь к человеку. Обращение не отправляется автоматически, история/документы не передаются, CRM не подключена.
- Вопросы о помощи/оплате/корзине сохраняют видимое предложение с отдельным согласием; сбой модели отменяет предложение, которое иначе стало бы скрытым. Контракт Pydantic/OpenAPI дополнен `suggestions`, `manager`, `website` и `GET /api/manager`.
- Живые файлы: семь входов дают правильные позиции и количества сразу; фото аппарата с мелким кодом требует уточнения, после явного кода найден правильный товар. Уточнение **не считается** прямым распознаванием фотографии. Исходник и первые неудачные попытки сохранены; ошибочное предположение о читаемости кода по URL карточки описано в `out/README.md`. Для JPEG накладной обе строки извлечены из изображения.
- Проверки: **41 backend**, **22 браузерных без внешних сервисов**; в живом браузере прошли 4 сценария UC/Excel и ещё 4 помощи/JPEG на desktop/mobile. Первый тест помощи ошибочно искал буквальное слово «подтверж», хотя ответ уже содержал действие «отдельно нажмите «Да, добавить»»; проверка исправлена на требуемое действие, затем повторена. Восемь файлов и четыре реплики проверены отдельно настоящими OpenAI/ekt.kz; Excel дал 3 товара / 6 единиц только после согласия. Протоколы — `out/verification.json` и `out/observed-session.json`.
- Ruff, ty, ESLint, TypeScript и генерируемые контракты проверены. Новых зависимостей нет; основные ТЗ и `plan.md` не изменены. README, ROADMAP, архитектура и инструкции по файлам/проверкам обновлены.

### Проверка спецификации и путь покупателя — UC-01/02/04/05

- В `index.html` и API добавлен разбор по позициям: запрос, количество, совпадение с каталогом и статус. Неизвестные строки и неопределённые совпадения остаются видимыми; пропущенное количество не становится единицей.
- Повторы и текущая корзина учитываются при проверке остатка. Готовые строки выбираются пользователем и группируются перед повторной серверной проверкой; отдельное согласие остаётся обязательным. Аналоги автоматически в партию не попадают.
- Публичные рейтинги удалены; README, ROADMAP и `out/` описывают использование, преимущества сценария и непроверенные ограничения. `docs/product-value.md` фиксирует план сравнения и измерения пользы без неподтверждённых рыночных заявлений.
- Три входа с лендинга сохранены: товар, спецификация, замена. Их предыдущая проверка: 16 браузерных без внешних API и 4 живых (46,2 с).
- Проверки: 31 backend, 18 браузерных без внешних API, дополнительно 2 после изменения прокрутки; 4 живых desktop/mobile за 45,3 с, включая XLSX с двумя позициями 2 + 1 и актуальную корзину. Ruff, ty, ESLint, TypeScript, форматирование и OpenAPI прошли.
- В первом живом прогоне отсутствовала локальная настройка каталога. Выданный доступ подключён только в игнорируемом `.env`, после чего сценарии прошли; интерфейс теперь явно показывает неподключённый каталог. Код и артефакты секретов не содержат.

### Исправлены повторное открытие корзины, формат условий и пропуски аналогов

- **UC-04/05:** содержимое корзины переносится в видимую часть того же чата. Убран конфликт с общей прокруткой после запроса и фокусировкой поля ввода; чтение корзины доступно во время обработки сообщения. Текстовый запрос корзины показывает позиции сразу, `/cart` и обновление страницы сохраняют состояние. Отмена при изменении количества передаёт ID старого предложения и не отменяет новое.
- **UC-03:** официальный HTML условий разбирается по разделам, с сохранением абзацев и пунктов. В ответ не попадают навигация, дубли и лишняя карточка последнего товара. UI безопасно отображает отдельные разделы оплаты, доставки и минимальной партии; различия версий источника обозначаются.
- **UC-02:** поиск аналогов выполняется для каждой отсутствующей позиции, включая несколько результатов и просьбу добавить отсутствующий товар. При пустом узком поиске используются тип товара и раздел каталога. Добавлены технические факты из названия, нормализация `1P+N`/`1П+Н`, `А`/`A`, дробных значений и компактных обозначений типа `C32`. Сравниваются назначение и известные параметры; аксессуары, другой ток и меньшая отключающая способность исключаются. Наличие проверяется API, совпадения/отличия объясняются; корзина не меняется при предложении замены.
- **Проверено:** 26 backend-тестов; 14 браузерных проверок без внешних сервисов; 4 живых прогона desktop/mobile с OpenAI и ekt.kz. Живой набор проверяет все пять UC, повторное открытие/удаление корзины и добавление распознанного CSV-товара с согласием. Для `ярп4520` и `310100080_` доступны аналоги без отдельной просьбы о замене; отдельно проверен подбор LED-светильников для `151000197_`. Ruff, ty, ESLint, TypeScript и контракт OpenAPI актуальны.
- **Оставшаяся граница UC-02:** минимум один подходящий аналог подтверждён на проверенных случаях, но не для каждого отсутствующего товара полного каталога. Для `RM35BA10` кандидата не найдено, включая поиск точной модели/серии. Это явно незакрытый случай в ROADMAP; выдуманный или нерелевантный товар не подставляется. Полная взаимозаменяемость иных моделей не обещается без данных о совместимости.

### Очистка до сценариев HackAlem и минимальной основы frontend

- **UC-01…05:** удалены 607 файлов неиспользуемого шаблона: SaaS-страницы/админка, организации, биллинг, общая авторизация, RAG, MCP, поиск/исполнение кода, почта, фоновые задачи, миграции и их тесты/инструкции. Удалены `README.template.md`, `TMP_ROADMAP.md`, временный HTML, генераторные правила и лишние варианты Compose. Исторические записи ниже могут упоминать эти ранее существовавшие файлы.
- **Рабочий backend:** сохранены `app.landing`, каталог и генераторы контрактов; `schemas/__init__.py` больше не импортирует удалённые модели. Объявления зависимостей сокращены 37 → 11, dev 7 → 3, lockfile 155 → 45 пакетов. Из окружения удалены 109 неиспользуемых пакетов.
- **Frontend по подтверждённому выбору заказчика:** Next.js 16.3.6, React, Tailwind CSS 4, один используемый shadcn Button и `cn`. Минимальная входная страница ведёт к действующему ассистенту через `ASSISTANT_URL`. Рабочие зависимости сокращены 45 → 7, dev 27 → 11; npm удалил 481 пакет. Добавлены `components.json` и npm lockfile; старый чат/авторизация/MDX/карты/графики удалены.
- **Запуск и поддержка:** Makefile, CI и один Docker Compose теперь обслуживают актуальные сценарии без БД/Redis. Образ собирается из корня, включает HTML и каталог, запускается непривилегированным пользователем; секреты и документы с доступом исключены из контекста. README, AGENTS и три оставленных технических документа соответствуют текущему коду.
- **Проверка после изменений:** 20 офлайн-тестов backend проверяют согласие, остатки, изоляцию, атомарность, срок предложения, удаление, схемы и семь существующих файлов. Повторно прошли 10 автономных и 4 живых браузерных прогона, включая UC-01…05; Next.js production build, Ruff, ty, ESLint, TypeScript и генераторы OpenAPI. Docker-образ собран; в контейнере без сети проверены каталог, HTML, сессия и корзина, запуск без root и отсутствие локального `.env`. Битых локальных ссылок Markdown нет.
- **Сохранность:** SHA-256 подтвердили отсутствие изменений в рабочем `index.html`, бизнес-коде ассистента, модели каталога, Word/JSON источниках, контрактах и `out/`. Локальные `.env` не переписаны. Перенос полного UI в React и постоянное хранилище остаются следующими этапами.


### Пакет сценариев использования и примеров `out/`

- По запросу заказчика создан [out/README.md](out/README.md): назначение папки, возможности текущего приложения, запуск, готовые входы и ожидаемые выходы. Источник — Word ТЗ, разделы 3–7 и 9–10.
- [Пошаговые UC-01…05](out/use-cases.md) сохраняют точное соответствие пяти Must have: факты, аналог, условия, отдельное согласие, ссылка на актуальную корзину. Файловый ввод описан как вариант этих сценариев. [Ожидаемые результаты](out/expected-results.md) отделены от реальных наблюдений и не подставляются в приложение.
- Сгенерированы DOCX, XLSX, PDF, PDF со сканом, JPEG, TXT и CSV: одни и те же коды `027005` и `027028`, синтетические количества 2 и 1. Реальные клиентские/платёжные данные, вымышленные цены и остатки отсутствуют. JPEG обозначен как изображение учебной спецификации, а не фото изделия.
- Генератор использует уже установленные зависимости; manifest фиксирует соответствие артикулам каталога, тип примера и контрольные суммы. Все семь файлов локально приняты текущим обработчиком; коды Excel сохраняют ведущие нули, скан PDF не содержит текстового слоя.
- **Проверка новых артефактов:** все семь файлов реально обработаны через OpenAI и каталог; найдены обе позиции, корзина без согласия осталась пустой. Записаны датированные `out/verification.json` и `out/observed-session.json`: пять UC, «ок» без изменения, отдельное «да, добавь», 2 + 1 шт. в актуальной корзине. Это новый API-прогон примеров; предыдущие 14 браузерных проверок не выдаются за повторно выполненные в этом этапе.
- **Прямой путь по документу:** XLSX → LLM извлекла обе позиции и количества 2/1 → предложение без изменения корзины → отдельное подтверждение → 3 единицы. Независимая сессия записана в `file_to_cart`; отчёт привязан к SHA-256 входных файлов. Проверены локальные ссылки Markdown, структурная валидность файлов и Ruff генератора.
- ROADMAP дополнен инвентаризацией изменений лендинга и отдельным этапом передачи сценариев. Исходное Word ТЗ и `plan.md` сохранены; актуальные инструкции находятся в `out/`.

### Детализация завершённого лендинга — UC-01…05

| Изменение | Пользовательский результат | Проверка |
| --- | --- | --- |
| Единый `index.html`: шапка, hero, четыре категории, блок возможностей, порядок работы/вложения, FAQ, подвал и кнопки чата | Посетитель открывает консультацию из разных блоков страницы; всё продолжается в одном окне | Playwright desktop/mobile, финальный визуальный просмотр |
| Текстовый ввод, быстрые запросы, прикрепление/удаление файлов, ожидание и ошибки | Понятный переход от текста или документа к ответу; исключена повторная отправка во время обработки | Проверки текстовых и файловых входов, отсутствие JS-ошибок |
| Товарные карточки: артикул, свойства автоматов/светильников, описание, склады, сертификат/его отсутствие, цена и время источника | Данные для выбора видны до добавления; противоречие 160/250 А явно показано | Сверка с настоящей карточкой, браузерные UC-01/02 |
| Количество, предложение с согласием, счётчик, удаление с подтверждением, `/cart` | Состав корзины контролирует посетитель; ссылка открывает актуальное состояние той же сессии | UC-04/05, «ок», отмена, повтор согласия, остатки и изоляция сессий |
| Адаптивный чат, сворачивание/Escape/фокус, подписи и экранирование текста | Работа с компьютера/телефона; сообщения не исполняются как HTML | Два размера экрана, проверка HTML-инъекции и переполнения |
| Явное разделение режима снимка и серверного режима; исходный `index(1).html` сохранён | Пользователь видит источник данных и режим корзины | 10 автономных и 4 живых браузерных проверки предыдущего этапа |

### Живой ассистент и серверная корзина — завершение сценариев лендинга

- **UC-01:** OpenAI Responses из `.env`, структурированный разбор свободного текста/контекста; поиск на ekt.kz и проверенные карточки. Новый артикул имеет приоритет над предыдущим товаром. Ссылки сертификатов извлекаются из данных/страницы, отсутствие документа не скрывается.
- **UC-02:** проверка совпадающих параметров и наличия; живой нулевой остаток `ярп4520` → доступный `150200474_`, объяснение полного совпадения обозначения и марки.
- **UC-03:** оплата и доставка из опубликованной страницы магазина; неизвестная общая минимальная партия и неоднозначные тарифные пороги обозначены.
- **UC-04/05:** по уточнению заказчика корзина теперь на нашем сервере; `CartAdapter` предусмотрен для будущей интеграции ekt.kz. Защищённая сессия, CSRF/Origin, одноразовое согласие с TTL, повторная проверка всего набора и прямая ссылка `/cart`. Заказ магазина не создаётся.
- **Вложения:** DOCX/XLS/XLSX разбираются на сервере, PDF/JPEG — моделью; извлечённые позиции проверяются по каталогу. Raw-файлы не сохраняются приложением. UI, карточки и корзина остаются в одном `index.html`.
- **Проверено после реализации, без TDD:** 10 автономных и 4 живых Playwright-прогона (UC-01…05 и вложение на desktop/mobile); отдельные запросы для DOCX, XLS, XLSX, текстового PDF, PDF со сканом, JPEG в новых сессиях. Проверены чужая сессия, CSRF/Origin, отмена, повторное согласие и переполнение. С управляемым изменением реальных карточек проверены истечение согласия и отсутствие частичного изменения при смене остатка одной позиции. Контракт Pydantic → OpenAPI добавлен в CI.
- **Границы:** сертификат с настоящей ссылкой в проверенной выборке не найден; старый DOC не проверен. Корзины в памяти одного процесса, очистка при перезапуске/часе бездействия. Полный индекс каталога и размещение на ekt.kz не заявляются.

Предыдущие записи ниже описывают раннюю стадию с автономной корзиной; актуальный статус — в README и ROADMAP.

| Выполненное изменение | Связь с UC и Word ТЗ | Проверка и текущая граница |
| --- | --- | --- |
| Завершён адаптивный [index.html](index.html): лендинг магазина и одно раскрывающееся окно чата; карточки, склады, количество, подтверждение и локальная корзина остаются в разговоре. Исходный `index(1).html` сохранён. | **UC-01…05:** пользовательские входы и состояния обязательных сценариев. | 10 проверок Playwright на десктопе и мобильном экране; скриншоты, отсутствие горизонтального переполнения и ошибок JavaScript. Корзина демонстрационная; аналоги — кандидаты для сравнения; неподтверждённые условия не выдумываются. |
| Добавлен отдельный FastAPI-шлюз `app.landing`, использующий существующие Pydantic-модели каталога и серверные `EKT_API_USER` / `EKT_API_PASSWORD`. HTML также открывается самостоятельно с помеченным снимком из JSON партнёра. | **UC-01:** живые факты о товаре; основа повторной проверки остатка для **UC-04**. | Живые карточка и страница 2 прошли валидацию; значения шлюза совпали с прямым ответом ekt.kz. Браузер: код `027228` → артикул `200300285_`, 23 шт., 6,9 с в одном прогоне. Источник сертификатов и полный поиск каталога не подтверждены. |
| В чат добавлены чтение DOCX, XLS/XLSX, текстового PDF и OCR надписей JPEG/PNG; удаление вложений и понятные ошибки формата/размера. | Входы раздела 5 ТЗ, **UC-01**. | Тестовые DOCX, XLSX, XLS, PDF, JPEG в браузере: извлечён `027228`, показана живая карточка `200300285_`. `.doc` требует пересохранения; сканы PDF и распознавание предмета по фото не закрыты. Языковая модель в лендинг пока не интегрирована. |
| Контракт партнёрского API и сгенерированный OpenAPI перенесены в [contracts/](contracts/README.md); карта папки показывает границы frontend, backend, каталога и БД без объявления новых интерфейсов готовыми. | **UC-01:** данные для первого ответа по товару; позднее **UC-02/04** используют тот же каталог. | `uv run python generate_ekt_catalog_openapi.py --check` прошёл после переноса; внутренние ссылки проверены. |
| Требования к пользовательским сценариям зафиксированы в PRD; шаги и измеримые результаты — в [ROADMAP.md](ROADMAP.md). [README.md](README.md) отделяет возможности шаблона от готовности ассистента. | **UC-01:** п. 1 — товар, характеристики, наличие, сертификат. **UC-02:** п. 2 — аналоги. **UC-03:** п. 3 — условия покупки. **UC-04:** п. 4 — подтверждаемое добавление. **UC-05:** п. 5 — ссылка на корзину. Входы и выходы — раздел 5 ТЗ; поля каталога — раздел 6. | Содержание сверено с Word ТЗ. Это фиксация требований, а не подтверждение выполнения UC. |
| Проверен предоставленный API каталога ekt.kz с Basic Auth; наблюдаемые поля, ошибки и расхождения с ТЗ описаны в [контракте каталога](contracts/ekt-catalog-api.md). Добавлены [Pydantic-модели](backend/app/schemas/ekt_catalog.py) и генерируемая [OpenAPI-схема](contracts/ekt-catalog.openapi.json). | **UC-01, п. 1:** источник артикула, свойств и остатков. **UC-02, п. 2:** данные для поиска аналога. **UC-04, п. 4:** данные для будущей проверки количества. | Живые запросы: 3 страницы списка, 13 детальных карточек и ответы `400`/`401`/`404`. Ответы списка, карточки и `404` прошли Pydantic-валидацию. Отдельное поле сертификата, условия покупки и API корзины пока не подтверждены. |
| В CI добавлена проверка воспроизводимости OpenAPI через `generate_ekt_catalog_openapi.py --check`; обновлены PRD, ROADMAP и README с фактическим статусом API. | Подготовка контракта данных прежде всего для **UC-01**, затем для **UC-02/04**; требования пунктов 1–5 Word ТЗ не изменены. | Локально прошли проверка генерации OpenAPI, ruff, ty и `git diff --check`. Интеграция «чат → API → ответ», поиск аналогов и изменение корзины ещё не проверены. |
| [Иерархия источников](<docs/source of truth/source of truth.md>) закрепляет итоговое Word ТЗ HackAlem AI как первичный источник требований. Переработаны [PRD](<docs/source of truth/PRD.md>), [Use Cases](<docs/source of truth/use-cases.md>), план и roadmap: главная задача и ключевой путь взяты из разделов 2–5 Word ТЗ, каждому пункту Must have назначен отдельный UC, UC-01 поставлен первым сквозным результатом. | **UC-01…05**, пункты 1–5 раздела 7 ТЗ; первичная приёмка относится к **UC-01**. | Проверены соответствие нумерации ТЗ и документации, ссылки на контракт API и отсутствие заявлений о готовности непроверенных UC. |
